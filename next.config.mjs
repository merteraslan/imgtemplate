/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special config needed since we're using a standalone API function
  // for the browser-based functionality
};

export default nextConfig;
